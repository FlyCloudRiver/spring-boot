<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper" >

    <!-- resultMap 指定表中字段和实体类中属性的映射-->
    <!-- property：主键在pojo中的属性名 -->
    <!-- column：主键在数据库中的列名 -->
    <resultMap id="userMapp" type="com.example.demo.entity.User">
        <!--主键用<id>标签，表示把表中的id字段映射到实体类User中的user_id属性-->
        <id column="user_id" property="userId"/>
        <!--非主键用<reslut>标签，表示把表中的name字段映射到实体类User的user_name属性-->
        <result column="user_name" property="userName"/>
        <result column="user_age" property="userAge"/>
    </resultMap>

    <select id="selectUserById"  resultMap="userMapp" parameterType="Integer">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <select id="find" resultMap="userMapp">
        select * from user where user_name=#{name} and user_password=#{password}
    </select>
</mapper>
