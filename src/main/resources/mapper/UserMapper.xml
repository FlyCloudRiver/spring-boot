<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper" >

    <!-- resultMap 指定表中字段和实体类中属性的映射-->
   <!-- <resultMap id="userMapp" type="com.example.demo.domain.User">
        &lt;!&ndash;主键用<id>标签，表示把表中的id字段映射到实体类User中的user_id属性&ndash;&gt;
        <id property="user_id" column="userId"/>
        &lt;!&ndash;非主键用<reslut>标签，表示把表中的name字段映射到实体类User的user_name属性&ndash;&gt;
        <result property="user_name" column="userName"/>
        <result property="user_age" column="userAge"/>
    </resultMap>-->

    <select id="selectUserById"  resultType="User">
        SELECT * FROM user WHERE user_id = #{user_id}
    </select>

    <select id="find" resultType="User">
        select * from user where user_name=#{name} and user_password=#{password}
    </select>
</mapper>
